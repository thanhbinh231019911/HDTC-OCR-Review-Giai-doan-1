# BÀN GIAO TIẾN ĐỘ PROJECT OCR HĐTC - 18/06/2026

## 1. Trạng thái đồng bộ

- Repository: `https://github.com/thanhbinh231019911/HDTC-OCR-Review-Giai-doan-1`
- Nhánh làm việc: `main`
- Thư mục trên máy hiện tại: `C:\Users\Administrator\Documents\HDTC-OCR-Review-Giai-doan-1`
- Apps Script project được liên kết bằng `.clasp.json`.
- Toàn bộ thay đổi code cuối phiên đã được commit và push lên `origin/main`.
- Commit code và bàn giao chính: `0626a10` - `Verify land registry OCR by crop consensus and hand off progress`.
- Đã chạy `npx.cmd clasp push --force` thành công lúc 16:08 ngày 18/06/2026; 21 file Apps Script đã được upload.
- Web App chính:
  - Deployment ID: `AKfycbyXkDTtk4PVPzjdCwy1duKEtbqJrNUHlbsF7TO_jTaMJ1JCCz8PJUf7vzerZijF1KyD`
  - Phiên bản đang deploy tại thời điểm kiểm tra: `@115`
  - Mô tả: `Remove accidental case overrides`

## 2. Công việc đã làm trong ngày 18/06/2026

Các commit đã hoàn thành trước phần sửa cuối phiên:

- `81ef96b` - đặt tên layout theo tiêu đề giấy chứng nhận.
- `bd2f8d7` - chia OCR bìa đất theo từng vùng Vision.
- `b2c2099` - xử lý PDF scan giấy chứng nhận bố cục A4.
- `6b7bf4e` - giữ độ trung thực của các trường giấy chứng nhận A4 mới.
- `573661f` - sửa các trường A4 còn thiếu bằng OCR crop.
- `804afb3` - dùng OCR đã lưu làm fallback cho trường giấy chứng nhận A4.
- `949a0b7` - repair dữ liệu review trước khi tự động lưu kết quả OCR.
- `3eba918` - chuẩn hóa mã giấy chứng nhận và khoảng trắng quanh dấu câu.
- `e74dc0f` - giới hạn repair đúng tài sản/file và xử lý trường hợp thay đổi không rõ.
- `265070f` - thêm endpoint xóa chính xác override phát sinh nhầm cho một case.

Phần sửa cuối phiên:

- Thay cơ chế lưu `Số vào sổ cấp GCN` từ auto OCR đơn lẻ sang cơ chế đồng thuận crop OCR.
- Chỉ chấp nhận mã khi:
  - mã có cấu trúc hợp lệ;
  - có ít nhất 2 crop đọc ra cùng một mã đầy đủ;
  - không có mã cạnh tranh có cùng số phiếu.
- Không ghi kết quả auto OCR vào bảng manual override.
- Không ghi đè khi trường đã có chỉnh sửa thủ công.
- Kiểm tra chống ghi đè dữ liệu đã thay đổi bằng `currentValue`.
- Lưu nguồn là `AUTO_OCR_LAND_REGISTRY_CROP_CONSENSUS`, confidence `0.92`, kèm audit log.
- Frontend tự kiểm tra crop cho các trường chưa mang nguồn xác minh mới.
- Bỏ endpoint xóa override tạm thời sau khi đã xử lý xong case lỗi.
- Cập nhật quy tắc OCR bìa đất để cấm hard-code mã hoặc học phép thay ký tự từ một ví dụ riêng lẻ.
- Bổ sung regression test cho các trường hợp đồng thuận, hòa phiếu và chỉ có một kết quả.

## 3. File thay đổi trong phần cuối phiên

- `Review.html`
- `ReviewService.gs`
- `ReviewWebApp.gs`
- `skills/ocr-bia-dat/references/core-rules.md`
- `tools/land_ocr_regression_test.js`
- `BAN_GIAO_2026-06-18.md`

## 4. Kiểm tra đã chạy

- `npm.cmd run test:land-ocr`: đạt, kết quả `OK land OCR regression`.
- Kiểm tra cú pháp JavaScript của:
  - `ReviewService.gs`
  - `ReviewWebApp.gs`
  - phần `<script>` trong `Review.html`
- `git diff --check`: không có lỗi whitespace làm hỏng patch.
- `git fetch origin`: remote không có commit mới; local trước khi bàn giao đang đi trước `origin/main` 2 commit.
- `npx.cmd clasp status`: các file Apps Script chính được clasp theo dõi.
- `npx.cmd clasp deployments`: Web App chính vẫn ở `@115`.

## 5. Công việc đang làm/dở

- Cơ chế đồng thuận crop OCR cho `Số vào sổ cấp GCN` đã hoàn tất ở code và regression test.
- Source đã có trên cả GitHub và Apps Script project.
- Cần kiểm thử bằng case thật có ảnh/PDF bìa đất, đặc biệt các trường hợp:
  - OCR cũ đọc nhầm một ký tự nhưng mã vẫn có vẻ hợp lệ;
  - nhiều crop cho cùng một mã;
  - hai mã khác nhau bị hòa phiếu;
  - trường đã được người dùng sửa tay;
  - case đã final.
- Source mới cần được deploy thành version mới của Web App nếu muốn người dùng thấy thay đổi trên link review. Không tạo deployment ID mới; cập nhật đúng deployment chính nêu ở mục 1.

## 6. Việc tiếp theo

1. Trên máy mới, clone hoặc pull `origin/main`, sau đó chạy `npm install`.
2. Chạy `npm.cmd run test:land-ocr`.
3. Đăng nhập clasp bằng đúng tài khoản Google: `npx.cmd clasp login`.
4. Kiểm tra `npx.cmd clasp status` và `npx.cmd clasp deployments`.
5. Test một case bìa đất thật trên Apps Script.
6. Nếu test đạt, tạo version và cập nhật deployment chính:

   ```powershell
   npx.cmd clasp version "Verify land registry number by crop OCR consensus"
   npx.cmd clasp deploy -i AKfycbyXkDTtk4PVPzjdCwy1duKEtbqJrNUHlbsF7TO_jTaMJ1JCCz8PJUf7vzerZijF1KyD -V <VERSION_MOI> -d "Verify land registry number by crop OCR consensus"
   ```

7. Kiểm tra lại trên link review rằng:
   - mã chỉ tự thay khi có tối thiểu 2 crop đồng thuận;
   - không thay manual override;
   - không lặp auto OCR sau khi trường đã mang nguồn consensus;
   - audit log ghi đúng mã cũ, mã mới và các readings.

## 7. Cách tiếp tục ở máy khác

Nếu chưa clone:

```powershell
git clone https://github.com/thanhbinh231019911/HDTC-OCR-Review-Giai-doan-1.git
cd HDTC-OCR-Review-Giai-doan-1
npm install
npx.cmd clasp login
npm.cmd run test:land-ocr
```

Nếu đã có repository:

```powershell
cd <THU_MUC_REPOSITORY>
git status --short
git pull --ff-only origin main
npm install
npm.cmd run test:land-ocr
npx.cmd clasp status
```

Không chạy `clasp pull` nếu máy đang có code local chưa commit, vì có thể ghi đè thay đổi. GitHub `origin/main` là nguồn đồng bộ code giữa các máy.

## 8. Nguyên tắc cần giữ

- Không suy diễn dữ liệu pháp lý khi OCR không chắc chắn.
- Không hard-code dữ liệu để sửa riêng một case.
- Manual override luôn có độ ưu tiên cao hơn auto OCR.
- Mọi auto repair phải giới hạn đúng case, đúng asset, đúng field và có bằng chứng truy vết.
- Không tạo Web App deployment mới nếu không có yêu cầu; cập nhật deployment chính hiện tại.

## 9. Cập nhật ngày 19/06/2026

- Đã sửa bộ chọn khối người sử dụng đất: chỉ nhận đầu mục in bắt đầu bằng `I.` hoặc `1. Người sử dụng đất`, loại nội dung `Người sử dụng đất thay đổi` tại mục IV.
- Hỗ trợ mẫu cũ có tên chủ sử dụng ở một dòng và `Năm sinh, CMND/CCCD số` ở dòng kế tiếp; giữ nguyên nhãn giấy tờ in trên GCN.
- Auto repair thông tin chủ sử dụng được giới hạn theo OCR của đúng asset/file, không dùng OCR của tài sản khác trong case nhiều tài sản.
- Đã thêm test hồi quy cho OCR nhiễu mục IV và hai chủ sử dụng trên mẫu GCN cũ.
- Case kiểm tra production: `HDTC-20260619-081547-F58754DC`.
- Kết quả production: `Trịnh Công Bách / CMND 113387328` và `Trần Thùy Linh / CMND 113565433`.
- Web App deployment chính đã cập nhật lên version `117`: `Fix land certificate owner section selection`.
- `npm.cmd run test:land-ocr`: đạt.
- Skill `skills/ocr-bia-dat`: hợp lệ qua `quick_validate.py`.

## 10. Cap nhat ngay 19/06/2026 - so vao so PDF

- Da sua luong auto OCR `So vao so cap GCN/Giay chung nhan` cho PDF nhieu trang:
  - Client render tung trang PDF bang PDF.js, khong dung thumbnail/preview trang 1 cua Drive lam bang chung.
  - Gui anh tung trang da scale ve server de Cloud Vision tim anchor `So vao so...` va `code_box` cua ma so vao so.
  - Tu `code_box`, client tao crop hep do doc/nam ngang va xoay crop dung huong truoc khi OCR.
  - Chi luu gia tri khi crop hep co 2 pass `color` va `gray` cung tra ve mot ma day du.
  - Cac crop rong/strip chi la fallback, khong duoc outvote crop hep.
- Source auto moi: `AUTO_OCR_LAND_REGISTRY_ANCHORED_PAGE_CROP_CONSENSUS`.
- Case dang xu ly: `HDTC-20260619-081547-F58754DC`.
  - OCR rong/cu doc thanh `CS03447`.
  - Kiem tra crop hep dung vung tren PDF cho ket qua dung bang mat: `CS03417`.
  - Logic moi khong hard-code `CS03417` va khong override rieng case; no chi sua khi crop neo theo nhan dat dong thuan.
- Da deploy Web App chinh len version `125`: `Anchor PDF registry number crop OCR`.
- Da kiem tra:
  - `npm.cmd run test:land-ocr`: OK.
  - GS syntax bang `vm.Script`: OK.
  - `Review.html` inline script syntax: OK.
  - `git diff --check`: OK.
  - Skill `skills/ocr-bia-dat`: OK qua `quick_validate.py`.
- Da them regression test cho client crop geometry:
  - `code_box` doc quanh toa do thuc te phai sinh crop hep xap xi vung `(30,850)-(650,1650)` va rotation `-90`.
  - Neu `CS03417` va `CS03447` hoa phieu thi khong tu chon.
  - Ma ngan/partial nhu `CS0147` bi loc neu khong cung do dai voi ma hien co.
- Chua the tu F5 web app bang browser Codex vi policy chan truy cap `script.google.com`.
  - Buoc tiep theo o moi truong thuc te: F5 case tren, cho 30-60 giay de auto crop chay.
  - Ky vong `So vao so` tu `CS03447` thanh `CS03417`, source thanh `AUTO_OCR_LAND_REGISTRY_ANCHORED_PAGE_CROP_CONSENSUS`.
  - Neu khong doi, kiem tra console/audit log cua action `AUTO_OCR_REGISTRY_SAVED` va cac readings crop.

## 11. Cap nhat ngay 19/06/2026 - F5 van giu CS03447

- Sau khi user F5, case van hien `CS03447`.
- Kiem tra API thay field dang o source cu `AUTO_OCR_LAND_REGISTRY_PAGE_CROP_CONSENSUS`.
- Nguyen nhan: asset trong review payload khong co `files`; fallback phai lay tu `ocr_results`. File PDF co ten viet tat `asset__GCN QSDD...pdf`/Drive co the la `secured_party__GCN QSDD...pdf`, trong khi fallback cu chi nhan cac cum day du nhu `giay chung nhan`, `quyen su dung dat`, `thua dat`.
- Da sua `getLandCertificateFilesFromOcr_()` de nhan PDF co ten chua `GCN`, `QSDD/QSD`, `bia dat`, `so do` bat ke prefix group, mien la co `file_id`.
- Da them regression test cho:
  - `asset__GCN QSDD - ...pdf` preview rong van duoc dua vao crop registry.
  - `secured_party__GCN QSDD - ...pdf` preview rong van duoc dua vao crop registry.
  - `obligor__CCCD.pdf` khong bi nhan nham.
- Da deploy Web App chinh len version `126`: `Detect GCN PDF fallback files for registry OCR`.
- Kiem tra:
  - `npm.cmd run test:land-ocr`: OK.
  - `Review.html` inline script syntax: OK.
  - `git diff --check`: OK.

## 14. Cap nhat ngay 22/06/2026 - sua CS0282D thanh CS02820 bang crop tight

- Case: `HDTC-20260619-161218-FAC7D8C6`.
- Loi: `So vao so` dang luu `CS0282D`, anh goc la `CS02820`.
- Tu test tren PDF production cho thay:
  - Crop `vertical_forward` rong doc `CS0282D` o ca color va gray.
  - Crop `vertical_forward_tight` cung dong viet tay doc `CS02820` o ca color va gray.
- Nguyen nhan: client dung ngay khi crop rong dau tien co color/gray trung nhau, nen chua bao gio xet crop tight chinh xac hon o phia sau.
- Da sua tong quat, khong hard-code ma va khong anh xa `D -> 0`:
  - Gom het focused crop cua mot trang truoc khi chon.
  - Neu co crop tight hop le thi chi xep hang cac crop tight; crop rong chi dung khi khong co ket qua tight.
  - Neu cac crop tight xung dot va hoa phieu thi khong tu sua.
  - Chi sau khi xet focused crop moi chay fallback crop rong.
- Da them regression test cho tinh huong hai crop rong cung sai `CS0282D` nhung crop tight doc `CS02820`, va test xung dot giua hai crop tight.
- Da deploy Web App chinh len version `131`: `Prefer tight registry crops after full page evaluation`.
- Da dung endpoint auto OCR, khong manual override, de luu ket qua crop that:
  - `So vao so`: `CS02820`.
  - Source: `AUTO_OCR_LAND_REGISTRY_ANCHORED_PAGE_CROP_CONSENSUS`.
  - Consensus: 2 readings (`color`, `gray`).
- Kiem tra:
  - `npm.cmd run test:land-ocr`: OK.
  - GS va `Review.html` inline script syntax: OK.
  - `git diff --check`: OK.

## 15. Cap nhat ngay 22/06/2026 - tach trigger OCR Lab khoi form production

- Loi production trigger: `Invalid review email for case HDTC-20260622-094444-CAAD662D. Raw value: ""`.
- Submission co cac cot `Email nhan ket qua lab` va `Upload anh bia dat/giay chung nhan`, nen day la form OCR Lab, khong phai form ho so hop dong production.
- Nguyen nhan: form Lab ghi response vao cung spreadsheet; vi vay ca form-specific trigger `onLabLandSubmit` va spreadsheet trigger `onFormSubmit` cung chay. `onFormSubmit` tao them case `HDTC` sai luong, map file Lab vao cac nhom production va loi khi gui email.
- Da them router dau `onFormSubmit`:
  - Nhan dien Lab bia dat va Lab CCCD tu ten cot upload.
  - Bo qua production handler voi reason `OCR_LAB_SUBMISSION`.
  - Form production van xu ly binh thuong.
- Da refactor detector dung chung voi router Lab va them regression test cho Lab bia dat, Lab CCCD, production.
- Da deploy Web App chinh len version `132`: `Route OCR lab submissions away from production handler`.
- Kiem tra:
  - `npm.cmd run test:land-ocr`: OK.
  - GS syntax: OK.
  - `git diff --check`: OK.

## 16. Cap nhat ngay 22/06/2026 - pipeline tong quat cho 3 truong quan trong va giu nguyen form A4

- Da tao tag rollback truoc khi sua: `backup-before-general-a4-field-crops-20260622` tai commit `9b12eef`.
- Case kiem tra: `LAB-ocr-bia-dat-20260622-095519-52FB8FDF`.
- Sua email Lab:
  - Email trong form duoc doc dung: `thanhbinh23101991@gmail.com`.
  - Nguyen nhan khong gui: handler Lab cu khong goi `sendReviewEmail`, du lai dat status `REVIEW_SENT`.
  - Handler Lab moi gui mail sau khi tao Review, ghi audit rieng va khong lam hong case neu mail loi.
  - Da gui lai thanh cong case hien tai luc `2026-06-22T04:35:00.446Z`.
- Sua ba pipeline doc truong quan trong, khong hard-code case:
  - `So GCN`: tim serial o vung duoi trang bang Vision bounding box; crop rieng; color va gray cung doc `AA02439787`.
  - `So vao so`: tim nhan `So vao so cap Giay chung nhan/GCN`, ghep cac OCR token lien ke tren cung dong de tao `code_box`; khong ep prefix/do dai theo gia tri cu.
  - `Ngay cap`: tim dong `ngay/thang/nam`; Vision OCR truoc, neu mat muc xanh thi dung crop mau + green-channel voi AI thi giac; chi luu khi hai bien the trung nhau.
  - Hinh phong dai cho thay chu viet tay la `CN03441`; cac dau cham mau den la dong cham in san nen duoc loai theo rule da thong nhat.
- Ket qua production sau khi tu crop va luu bang endpoint AUTO_OCR:
  - `So GCN`: `AA02439787`
  - `So vao so`: `CN03441`
  - `Ngay cap`: `11/06/2025`
  - Cac source deu la crop consensus, khong manual override.
- Sua ton trong nguyen van:
  - Review A4 uu tien `certificate_owner_raw_text`; khong tu them `chong/vo`.
  - `Va chong: Do Thanh Tuan, CCCD: 001070022821` khong con bi lap.
  - Muc `3. Thong tin tai san gan lien voi dat` tach theo nhan, khong theo thu tu OCR: `a. Ten tai san`, `b. Dien tich su dung`, `c. Hinh thuc so huu`, `d. Thoi han so huu`.
  - Neu OCR khong doc duoc gia tri `d`, review van giu nhan va de trong; khong tu suy luan `-/-`.
- Da sua phan loai trang 2 A4 de marker `4/5/6` khong bi rule old-style change chiem uu tien.
- Da deploy:
  - version `133`: `Generalize anchored land critical field OCR`
  - version `134`: `Handle split registry tokens and colored handwritten dates`
  - version `135`: `Finalize lab email and anchored land OCR`
- Kiem tra:
  - `npm.cmd run test:land-ocr`: OK.
  - GS va `Review.html` inline syntax: OK.
  - Skill `ocr-bia-dat`: valid.
  - `git diff --check`: OK.
  - Browser Codex bi enterprise policy chan `script.google.com`; da kiem tra production qua cung Review API ma giao dien su dung.

## 13. Cap nhat ngay 19/06/2026 - ngay cap va field xuong dong tong quat

- Case: `HDTC-20260619-161218-FAC7D8C6`.
- Loi user bao:
  - `Ngay cap` bi doc sai `19/01/2018`, anh doc dung la `19/07/2018`.
  - `Thua dat so` bi lay thanh `1.` thay vi `10`.
  - `Nguon goc su dung` bi cut mat phan xuong dong/bi OCR day lech dong.
- Nguyen nhan:
  - Full OCR doc dong ngay cap sai thang; day la truong viet tay phai crop rieng.
  - OCR text order bi dao: so thua `10` roi vao dong rieng, parser cu bam nham chi muc `1. Thua dat`.
  - `Nguon goc su dung` co cac manh continuation nam truoc/sau label trong cung land block.
- Da sua tong quat:
  - Them crop OCR rieng cho `assets[].real_estate.issue_date`, source `AUTO_OCR_LAND_ISSUE_DATE_CROP_CONSENSUS`.
  - Them endpoint `suggestLandIssueDateCropFromImage` va `saveAutoOcrIssueDateValue`.
  - Bao ve source crop ngay cap, khong cho repair full OCR ghi de lai.
  - Parser `Thua dat so` bo ung vien nghi la chi muc muc lon, doc them so doc lap gan label/truoc `to ban do so`, xu ly OCR `lo/l0/1o` thanh `10`.
  - Parser `Nguon goc su dung` gom continuation theo semantic trong field: chuyen nhuong, cong nhan QSDD, Nha nuoc giao dat, co/khong thu tien su dung dat; khong lay nha o/dia chi/dien tich/hinh thuc so huu.
  - Skill `ocr-bia-dat` da bo sung nguyen tac field nhieu dong va crop truong viet tay quan trong.
- Tu test production:
  - Render PDF/crop page 2 doc duoc `19/07/2018` ca color va gray.
  - API review sau sua:
    - `Ngay cap`: `19/07/2018`
    - `Thua dat so`: `10`
    - `Nguon goc su dung`: `Nhan chuyen nhuong dat duoc Cong nhan QSDD nhu giao dat co thu tien su dung dat`
    - Source ngay cap: `AUTO_OCR_LAND_ISSUE_DATE_CROP_CONSENSUS`.
- Da deploy Web App chinh len version `130`: `Protect cropped land issue dates from text repair`.
- Kiem tra:
  - `npm.cmd run test:land-ocr`: OK.
  - GS syntax: OK.
  - `Review.html` inline script syntax: OK.
  - `git diff --check`: OK.

## 12. Cap nhat ngay 19/06/2026 - test crop that va luu CS03417

- User bao F5 van giu `CS03447`; dung bang mat phai la `CS03417`.
- Da tu test bang API:
  - Tai PDF qua `getCasePdfData`.
  - Render tung trang PDF local.
  - Gui page image vao `suggestLandRegistryCropFromImage`.
  - Page 2 tim duoc `code_box` quanh dong `So vao so cap GCN`.
  - Crop hep `vertical_forward_tight_90` doc duoc `CS03417` o ca 2 pass `color` va `gray`.
- Ket luan: OCR/crop dung da co, nhung client cu tiep tuc OCR qua nhieu crop fallback nen qua cham/im lang; user F5 khong thay doi.
- Da sua `readLandRegistryFromAssetFiles_()`:
  - Khi mot crop anchored co `color + gray` cung ra mot ma hop le, tra ket qua ngay de goi save.
  - Khong doi tiep cac crop rong/fallback sau khi da co dong thuan crop hep.
- Da deploy Web App chinh len version `127`: `Save anchored registry OCR immediately`.
- Da dung chinh endpoint auto OCR de luu ket qua crop that cho case:
  - `saveAutoOcrRegistryValue` voi readings `CS03417`, `CS03417`.
  - Ket qua: `So vao so` = `CS03417`.
  - Source: `AUTO_OCR_LAND_REGISTRY_ANCHORED_PAGE_CROP_CONSENSUS`.
  - Evidence: `Anchored high-resolution page crop consensus: color and grayscale`.
- Kiem tra:
  - `npm.cmd run test:land-ocr`: OK.
  - `Review.html` inline script syntax: OK.
  - `git diff --check`: OK.
