# BÀN GIAO TIẾN ĐỘ PROJECT OCR HĐTC - 18/06/2026

## 1. Trạng thái đồng bộ

- Repository: `https://github.com/thanhbinh231019911/HDTC-OCR-Review-Giai-doan-1`
- Nhánh làm việc: `main`
- Thư mục trên máy hiện tại: `C:\Users\Administrator\Documents\HDTC-OCR-Review-Giai-doan-1`
- Apps Script project được liên kết bằng `.clasp.json`.
- Toàn bộ thay đổi code cuối phiên đã được commit và push lên `origin/main`.
- Commit code và bàn giao chính: `0626a10` - `Verify land registry OCR by crop consensus and hand off progress`.
- Đã chạy `npx.cmd clasp push --force` thành công lúc 16:08 ngày 18/06/2026; 21 file Apps Script đã được upload.
- Web App chính:
  - Deployment ID: `AKfycbyXkDTtk4PVPzjdCwy1duKEtbqJrNUHlbsF7TO_jTaMJ1JCCz8PJUf7vzerZijF1KyD`
  - Phiên bản đang deploy tại thời điểm kiểm tra: `@115`
  - Mô tả: `Remove accidental case overrides`

## 2. Công việc đã làm trong ngày 18/06/2026

Các commit đã hoàn thành trước phần sửa cuối phiên:

- `81ef96b` - đặt tên layout theo tiêu đề giấy chứng nhận.
- `bd2f8d7` - chia OCR bìa đất theo từng vùng Vision.
- `b2c2099` - xử lý PDF scan giấy chứng nhận bố cục A4.
- `6b7bf4e` - giữ độ trung thực của các trường giấy chứng nhận A4 mới.
- `573661f` - sửa các trường A4 còn thiếu bằng OCR crop.
- `804afb3` - dùng OCR đã lưu làm fallback cho trường giấy chứng nhận A4.
- `949a0b7` - repair dữ liệu review trước khi tự động lưu kết quả OCR.
- `3eba918` - chuẩn hóa mã giấy chứng nhận và khoảng trắng quanh dấu câu.
- `e74dc0f` - giới hạn repair đúng tài sản/file và xử lý trường hợp thay đổi không rõ.
- `265070f` - thêm endpoint xóa chính xác override phát sinh nhầm cho một case.

Phần sửa cuối phiên:

- Thay cơ chế lưu `Số vào sổ cấp GCN` từ auto OCR đơn lẻ sang cơ chế đồng thuận crop OCR.
- Chỉ chấp nhận mã khi:
  - mã có cấu trúc hợp lệ;
  - có ít nhất 2 crop đọc ra cùng một mã đầy đủ;
  - không có mã cạnh tranh có cùng số phiếu.
- Không ghi kết quả auto OCR vào bảng manual override.
- Không ghi đè khi trường đã có chỉnh sửa thủ công.
- Kiểm tra chống ghi đè dữ liệu đã thay đổi bằng `currentValue`.
- Lưu nguồn là `AUTO_OCR_LAND_REGISTRY_CROP_CONSENSUS`, confidence `0.92`, kèm audit log.
- Frontend tự kiểm tra crop cho các trường chưa mang nguồn xác minh mới.
- Bỏ endpoint xóa override tạm thời sau khi đã xử lý xong case lỗi.
- Cập nhật quy tắc OCR bìa đất để cấm hard-code mã hoặc học phép thay ký tự từ một ví dụ riêng lẻ.
- Bổ sung regression test cho các trường hợp đồng thuận, hòa phiếu và chỉ có một kết quả.

## 3. File thay đổi trong phần cuối phiên

- `Review.html`
- `ReviewService.gs`
- `ReviewWebApp.gs`
- `skills/ocr-bia-dat/references/core-rules.md`
- `tools/land_ocr_regression_test.js`
- `BAN_GIAO_2026-06-18.md`

## 4. Kiểm tra đã chạy

- `npm.cmd run test:land-ocr`: đạt, kết quả `OK land OCR regression`.
- Kiểm tra cú pháp JavaScript của:
  - `ReviewService.gs`
  - `ReviewWebApp.gs`
  - phần `<script>` trong `Review.html`
- `git diff --check`: không có lỗi whitespace làm hỏng patch.
- `git fetch origin`: remote không có commit mới; local trước khi bàn giao đang đi trước `origin/main` 2 commit.
- `npx.cmd clasp status`: các file Apps Script chính được clasp theo dõi.
- `npx.cmd clasp deployments`: Web App chính vẫn ở `@115`.

## 5. Công việc đang làm/dở

- Cơ chế đồng thuận crop OCR cho `Số vào sổ cấp GCN` đã hoàn tất ở code và regression test.
- Source đã có trên cả GitHub và Apps Script project.
- Cần kiểm thử bằng case thật có ảnh/PDF bìa đất, đặc biệt các trường hợp:
  - OCR cũ đọc nhầm một ký tự nhưng mã vẫn có vẻ hợp lệ;
  - nhiều crop cho cùng một mã;
  - hai mã khác nhau bị hòa phiếu;
  - trường đã được người dùng sửa tay;
  - case đã final.
- Source mới cần được deploy thành version mới của Web App nếu muốn người dùng thấy thay đổi trên link review. Không tạo deployment ID mới; cập nhật đúng deployment chính nêu ở mục 1.

## 6. Việc tiếp theo

1. Trên máy mới, clone hoặc pull `origin/main`, sau đó chạy `npm install`.
2. Chạy `npm.cmd run test:land-ocr`.
3. Đăng nhập clasp bằng đúng tài khoản Google: `npx.cmd clasp login`.
4. Kiểm tra `npx.cmd clasp status` và `npx.cmd clasp deployments`.
5. Test một case bìa đất thật trên Apps Script.
6. Nếu test đạt, tạo version và cập nhật deployment chính:

   ```powershell
   npx.cmd clasp version "Verify land registry number by crop OCR consensus"
   npx.cmd clasp deploy -i AKfycbyXkDTtk4PVPzjdCwy1duKEtbqJrNUHlbsF7TO_jTaMJ1JCCz8PJUf7vzerZijF1KyD -V <VERSION_MOI> -d "Verify land registry number by crop OCR consensus"
   ```

7. Kiểm tra lại trên link review rằng:
   - mã chỉ tự thay khi có tối thiểu 2 crop đồng thuận;
   - không thay manual override;
   - không lặp auto OCR sau khi trường đã mang nguồn consensus;
   - audit log ghi đúng mã cũ, mã mới và các readings.

## 7. Cách tiếp tục ở máy khác

Nếu chưa clone:

```powershell
git clone https://github.com/thanhbinh231019911/HDTC-OCR-Review-Giai-doan-1.git
cd HDTC-OCR-Review-Giai-doan-1
npm install
npx.cmd clasp login
npm.cmd run test:land-ocr
```

Nếu đã có repository:

```powershell
cd <THU_MUC_REPOSITORY>
git status --short
git pull --ff-only origin main
npm install
npm.cmd run test:land-ocr
npx.cmd clasp status
```

Không chạy `clasp pull` nếu máy đang có code local chưa commit, vì có thể ghi đè thay đổi. GitHub `origin/main` là nguồn đồng bộ code giữa các máy.

## 8. Nguyên tắc cần giữ

- Không suy diễn dữ liệu pháp lý khi OCR không chắc chắn.
- Không hard-code dữ liệu để sửa riêng một case.
- Manual override luôn có độ ưu tiên cao hơn auto OCR.
- Mọi auto repair phải giới hạn đúng case, đúng asset, đúng field và có bằng chứng truy vết.
- Không tạo Web App deployment mới nếu không có yêu cầu; cập nhật deployment chính hiện tại.

## 9. Cập nhật ngày 19/06/2026

- Đã sửa bộ chọn khối người sử dụng đất: chỉ nhận đầu mục in bắt đầu bằng `I.` hoặc `1. Người sử dụng đất`, loại nội dung `Người sử dụng đất thay đổi` tại mục IV.
- Hỗ trợ mẫu cũ có tên chủ sử dụng ở một dòng và `Năm sinh, CMND/CCCD số` ở dòng kế tiếp; giữ nguyên nhãn giấy tờ in trên GCN.
- Auto repair thông tin chủ sử dụng được giới hạn theo OCR của đúng asset/file, không dùng OCR của tài sản khác trong case nhiều tài sản.
- Đã thêm test hồi quy cho OCR nhiễu mục IV và hai chủ sử dụng trên mẫu GCN cũ.
- Case kiểm tra production: `HDTC-20260619-081547-F58754DC`.
- Kết quả production: `Trịnh Công Bách / CMND 113387328` và `Trần Thùy Linh / CMND 113565433`.
- Web App deployment chính đã cập nhật lên version `117`: `Fix land certificate owner section selection`.
- `npm.cmd run test:land-ocr`: đạt.
- Skill `skills/ocr-bia-dat`: hợp lệ qua `quick_validate.py`.
